import React from 'react'
import './Generator.scss'

const GeneratorInput = ({children}) => (
        <div className="generator__input">
          {children}
        </div>
)

export default GeneratorInput
